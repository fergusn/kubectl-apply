name: 'kubectl apply'
description: 'Deploy to Kubernetes with kubectl'
author: 'fergusn'
inputs: 
  filename:
    description: 'Filename that contains the configuration to apply'
runs:
  using: 'docker'
  image: 'fergusn/kubectl-apply/apply'
  args:
  - --filename
  - ${{ inputs.filename }}
